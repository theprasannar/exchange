{
  "name": "@exchange/db",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "prisma generate && tsc -p tsconfig.json",
    "build": "prisma generate && tsc -p tsconfig.json",
    "prisma:deploy": "prisma migrate deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.13.0",
    "date-fns": "^4.1.0",
    "dotenv": "^16.4.7",
    "pg": "^8.13.1",
    "redis": "^4.7.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "@types/redis": "^4.0.10"
  }
}
